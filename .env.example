# Remote Git MCP Configuration
# Copy this file to .env and adjust the values as needed

# Git repository URL (if a git repository already exists in the workspace, this can be left empty)
GIT_REPO_URL=
# Git repository workspace path (local full path)
WORKSPACE=
